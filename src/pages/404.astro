---
import Layout from '@/layouts/Layout.astro'
import ButtonLink from '@/components/ui/ButtonLink.astro'
import { Home, FileQuestion, ArrowRight } from 'lucide-react'
---

<Layout
  title="Página no encontrada | AIME - Asociación de Ingenieros de Minas del Ecuador"
  description="Lo sentimos, la página que buscas no existe o ha sido movida."
>
  <main class="pb-16 pt-10 bg-gradient-to-l from-white via-primary/5 to-white">
    <div class="container">
      <div class="flex flex-col items-center justify-center text-center">
        <!-- Número 404 estilizado -->
        <div class="relative mb-5">
          <p class="text-2xl font-bold text-center">OOPS!</p>
          <h1
            class="bg-gradient-to-r from-primary/70 via-primary to-primary/60 bg-clip-text text-[120px] font-bold leading-none text-transparent md:text-[150px]"
          >
            404
          </h1>
        </div>

        <!-- Mensaje principal -->
        <h2 class="mb-4 text-2xl font-bold text-secondary md:text-2xl">
          ¡Vaya! Esta página parece haberse extraviado
        </h2>
        <p class="mx-auto mb-8 max-w-2xl text-gray-600">
          Lo sentimos, la página que estás buscando no existe o ha sido movida.
          Es posible que hayas seguido un enlace desactualizado o que hayas
          escrito una URL incorrecta.
        </p>

        <!-- Ilustración minera -->
        <div class="mx-auto mb-10 w-full max-w-md shadow-md">
          <div class="rounded-lg bg-muted p-6 shadow-inner">
            <div class="mb-6 flex items-center gap-4">
              <div class="rounded-full bg-primary/20 p-3">
                <FileQuestion className="text-primary-dark h-8 w-8" />
              </div>
              <div class="flex-1 text-left">
                <h3 class="text-lg font-semibold">
                  ¿Necesitas ayuda para encontrar algo?
                </h3>
                <p class="text-sm text-gray-600">
                  Utiliza los enlaces a continuación para encontrar lo que
                  buscas.
                </p>
              </div>
            </div>

            <div class="flex flex-col justify-center gap-3 md:flex-row">
              <ButtonLink
                href="/"
                variant="default"
                class="flex items-center justify-center gap-2"
              >
                <Home className="h-4 w-4" /> Ir a inicio
              </ButtonLink>
              <ButtonLink
                href="/contacto"
                variant="outline"
                class="group flex items-center justify-center gap-2"
              >
                Contactar con nosotros <ArrowRight
                  className="h-4 w-4 transition-transform duration-300 group-hover:translate-x-1"
                />
              </ButtonLink>
            </div>
          </div>
        </div>
      </div>
    </div>
  </main>
</Layout>
