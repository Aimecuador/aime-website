---
import Layout from "@/layouts/Layout.astro";
import ButtonLink from "@/components/ui/ButtonLink.astro";
import { Home, ArrowLeft, Search, FileQuestion } from "lucide-react";

---

<Layout
  title="Página no encontrada | AIME - Asociación de Ingenieros de Minas del Ecuador"
  description="Lo sentimos, la página que buscas no existe o ha sido movida."
>
  <main class="pb-16 pt-10">
    <div class="container">
      <div class="flex flex-col items-center justify-center text-center">
        <!-- Número 404 estilizado -->        <div class="relative mb-8">
          <h1 class="text-[120px] md:text-[180px] lg:text-[220px] font-bold text-primary/10 leading-none">
            404
          </h1>
        </div>

        <!-- Mensaje principal -->
        <h2 class="text-2xl md:text-3xl lg:text-4xl font-bold mb-4 text-secondary">
          ¡Vaya! Esta página parece haberse extraviado
        </h2>
        <p class="text-gray-600 text-lg md:text-xl max-w-2xl mx-auto mb-10">
          Lo sentimos, la página que estás buscando no existe o ha sido movida. 
          Es posible que hayas seguido un enlace desactualizado o que hayas escrito 
          una URL incorrecta.
        </p>

        <!-- Ilustración minera -->
        <div class="w-full max-w-md mx-auto mb-10">
          <div class="bg-muted rounded-lg p-6 shadow-inner">
            <div class="flex gap-4 mb-6 items-center">
              <div class="bg-primary/20 p-3 rounded-full">
                <FileQuestion className="w-8 h-8 text-primary-dark" />
              </div>
              <div class="flex-1 text-left">
                <h3 class="text-lg font-semibold">¿Necesitas ayuda para encontrar algo?</h3>
                <p class="text-sm text-gray-600">Utiliza los enlaces a continuación para encontrar lo que buscas.</p>
              </div>
            </div>
            
            <div class="flex flex-col md:flex-row gap-3 justify-center">
              <ButtonLink href="/" variant="default" class="flex items-center justify-center gap-2">
                <Home className="w-4 h-4" /> Ir a inicio
              </ButtonLink>
              <ButtonLink href="/contacto" variant="outline" class="flex items-center justify-center gap-2">
                <ArrowLeft className="w-4 h-4" /> Contactar con nosotros
              </ButtonLink>
            </div>
          </div>
        </div>
      </div>
    </div>
  </main>
</Layout>
